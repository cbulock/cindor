const meta = {
  title: "Display/Splitter Panel",
  render: () => `
    <emb-splitter-panel style="padding: var(--space-4); border: 1px solid var(--border); border-radius: var(--radius-lg);">
      Standalone panel content
    </emb-splitter-panel>
  `
};

export default meta;

export const Default = {};
